version: '3.4'
services:
  app:
    image: luizimcpi/aws-publisher-service
    environment:
      - SPRING_PROFILES_ACTIVE=local
      - AWS_ACCESS_KEY_ID=foo
      - AWS_SECRET_ACCESS_KEY=bar
    container_name: aws-publisher-service-app
    ports:
      - 8080:8080
#    networks:
#      - my-network
#    depends_on:
#      - localstack
#    links:
#      - localstack

#networks:
#  my-network:
#    external: true